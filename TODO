- if (( a && b ) || c) by Pavel Golchouk
- rewrite string2addr()
- routeMail, route, routeFile must be "the same"
- getArea(TP_LOCAL|TP_ECHO|TP_BAD|TP_DUPE|TP_FILE)
- fix "0" groups (to NULL)
- fix "-manual" vs. "-mandatory": 
  manual means: subscription/unsubscription not allowed
  mandatory means: subscription allowed, unsubscription not allowed
- print name of config file with error

- add to doc & tparser for hpt
    what is [module]
    set, if, if not, else, elseif, ifdef, ifndef, endif

- for htick developers (add to doc, tparser, dumpcfg?):
    filefix
    filefixkillreports
    filefixkillrequests
    semadir
    badfilesdir
    bbsarea
    autofilecreate
    autofilecreatedefaults
    savetick
    execonfile
    origininannounce
    busyFileDir
    -sendorig
    ConvertShortNames
    ConvertLongNames
    forwardFileRequestFile
    ForwardFilePriority
    forwardFileRequests
    filefixhelp

- fix dumping of: Unpack, netMailArea, dupeArea, badArea, echoArea, origin,
tearline, remaps
